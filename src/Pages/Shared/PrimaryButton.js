import React from 'react';

const PrimaryButton = ({children}) => {
    return (
        <button class="btn btn-primary uppercase text-white  bg-gradient-to-r from-indigo-500 ...">{children}</button>
    );
};

export default PrimaryButton;